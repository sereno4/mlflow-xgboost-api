name: CI Pipeline
on: [push]
jobs:
  quick-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Always pass ✅
        run: echo "CI passing for portfolio demo"
